---
name: sample-app

templates:
  bpm.yml: config/bpm.yml

packages:
  - sample-app

properties:
  port:
    description: |
      The TCP port on which the application is listening for incoming HTTP
      requests.
    default: 20014
